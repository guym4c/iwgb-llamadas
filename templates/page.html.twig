<!doctype html>
<html>
	<head>
		<meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/css/style.css">
		<link href="https://fonts.googleapis.com/css?family=Source+Code+Pro:400,700" rel="stylesheet">
        <title>IWGB Llamadas</title>
	</head>
	<body>
		<div class="nav">
			<div class="content-pad">
				<img src="/img/iwgb-red-wordmark.png">
				<h1>llamadas📞🤙</h1>
			</div>
		</div>
		<div class="content-pad">
			{% block content %} {% endblock %}
        </div>
        <div class="content-pad">
            {% block languages %}
                <ul class="languages">
                    {% for language in languages %}
                        <li><a href="/{{ language.value }}{{ uri }}">{{ language.value|upper }}</a></li>
                    {% endfor %}
                    <li><a href="/logout">{{ copy.logout[_lang] }}</a></li>
                </ul>
            {% endblock %}
		</div>
	</body>
</html>