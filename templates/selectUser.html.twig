{% extends 'page.html.twig' %}

{% block content %}
	<div>
		<h2>{{ copy.identify[_lang] }}</h2>
		{% for caller in callers %}
			<p><a href="/{{ campaign }}/{{ caller.id }}">{{ caller.name }}</a></p>
		{% endfor %}
	</div>
{% endblock %}